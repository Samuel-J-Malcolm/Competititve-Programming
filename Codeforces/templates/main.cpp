/*


*/

#include <bits/stdc++.h>
using namespace std;

using ll = long long;

void solve()
{
    int n = 1;
    cin >> n;
    vector<int> v;
    int x, y;
    cin >> y;
    while (y--){
        cin >> x;
        v.push_back(x);
    }
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t = 1;
    cin >> t;
    while (t--)
        solve();

    return 0;
}
